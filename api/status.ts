const json=(data:unknown,status=200)=>Response.json(data,{status,headers:{'Cache-Control':'no-store'}});
const authorized=(request:Request)=>{const expected=process.env.API_TOKEN?.trim()??'';const received=request.headers.get('authorization')?.replace(/^Bearer\s+/i,'')??'';return Boolean(expected)&&received===expected};
const safeError=(error:unknown)=>{const message=error instanceof Error?error.message:String(error);if(/relation .* does not exist|worker_state/i.test(message))return{code:'DATABASE_SCHEMA_MISSING',message};if(/DATABASE_URL|connection string|connect|fetch failed|password|certificate|ENOTFOUND|ECONN/i.test(message))return{code:'DATABASE_CONNECTION_FAILED',message};return{code:'MODULE_OR_QUERY_FAILED',message}};
export default{async fetch(request:Request){if(!authorized(request))return json({error:'Unauthorized'},401);try{const{sql}=await import('../src/db');const rows=await sql`SELECT * FROM worker_state WHERE id=1`;return json({ok:true,worker:rows[0]??null})}catch(error){return json({ok:false,error:safeError(error)},500)}}};
